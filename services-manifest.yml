# Generated services-manifest.yml based on template version 0.1.0
# appName = cap-s4
---
create-services:
  # ------------------------------------------------------------
  - name: cap-s4-uaa
    broker: xsuaa
    plan: "broker"

  - name: cap-s4-destination
    broker: destination
    plan: "lite"
